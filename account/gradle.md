**bootRun:** run Spring Boot application locally if you want to test during development
```bash
./gradlew bootRun
```

**bootJar:** generate JAR artifact that contains Spring Boot application and his dependencies
```bash
./gradlew bootJar
```